// This component was replaced by `BacteriaCanvas` and a DOM-based
// `bacteria-container` in index.html. Keeping a lightweight stub
// so imports won't break older references. It intentionally renders
// nothing.
export default function BacteriaBackground() {
  return null
}
